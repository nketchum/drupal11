# DDev Notes

Here are some notes about this lightly customized config for DDev, which
includes initial project config and DDev module installation.

1. Execute or refer to /scripts/bash/ddev-setup.sh for project setup
steps and addons.
2. Enabled Xdebug if desired: $ ddev xdebug on
3. Check Xdebug status if desired: $ ddev xdebug status
